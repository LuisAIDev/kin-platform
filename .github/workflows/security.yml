name: Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  codeql:
    name: CodeQL (Java, JavaScript, TypeScript)
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: ["java-kotlin", "javascript-typescript"]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}

      - name: Make Maven Wrapper executable
        if: matrix.language == 'java-kotlin'
        working-directory: kin-backend
        run: chmod +x mvnw

      - name: Build backend (autobuild java)
        if: matrix.language == 'java-kotlin'
        working-directory: kin-backend
        run: ./mvnw -B -q -DskipTests package

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v3

  gitleaks:
    name: Secrets scan (Gitleaks)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  owasp-dependency-check:
    name: OWASP Dependency Check (backend)
    runs-on: ubuntu-latest
    # No bloquea el pipeline: requiere acceso a NVD (red externa). Revisar el reporte.
    # Solo se ejecuta cuando existe el Secret NVD_API_KEY; si no existe, se omite
    # con un warning (nunca falla por clave vacía).
    continue-on-error: true
    env:
      NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Make Maven Wrapper executable
        working-directory: kin-backend
        run: chmod +x mvnw

      - name: Run OWASP Dependency-Check
        if: "${{ env.NVD_API_KEY != '' }}"
        working-directory: kin-backend
        run: ./mvnw -B -q org.owasp:dependency-check-maven:check -DskipTests -DnvdApiKey="${NVD_API_KEY}"

      - name: OWASP omitido (NVD_API_KEY no configurada)
        if: "${{ env.NVD_API_KEY == '' }}"
        run: 'echo "::warning::OWASP Dependency Check omitido: el Secret NVD_API_KEY no está configurado."'

      - name: Upload report
        if: "${{ always() && env.NVD_API_KEY != '' }}"
        uses: actions/upload-artifact@v4
        with:
          name: owasp-report
          path: kin-backend/target/dependency-check-report.html
