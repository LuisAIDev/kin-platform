# Lombok - marcar el codigo generado con @lombok.Generated para que JaCoCo
# (y otras herramientas) lo excluyan de la cobertura: los getters/setters/
# builders no representan logica de negocio y no deben penalizar el ratio.
lombok.addLombokGeneratedAnnotation = true
